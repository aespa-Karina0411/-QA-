事件一致性审计
================================================================================

数据来源: analysis/full_run.jsonl
条目总数: 410
VLM SUBMIT 总数: 92
VLM PLAY 总数: 27
VLM 未播放: 65

一致性检查:
------------------
1. 每个 trace_id 的 SUBMIT 次数:
   仿真使用 uid() 生成 id，每次调用递增，无重复。✅

2. SELECT 标记:
   Aging boost 条目: 7 条标记了 aging_boost + _select_time  ✅
   Force play 条目: 11 条标记了 force_play  ✅

3. 是否存在 SELECT 但无 SUBMIT:
   所有 aged/forced 条目均有 submit 阶段  ✅

4. 是否存在 PLAY 但无 SELECT:
   仿真中 select 和 play 发生在同一 drain 周期内，select_ts 近似 = play_ts - 2.5s
   所有 played 条目均可在 trace 中追溯到 drain 链  ✅

5. 未播放条目分类:
   65 条未播放 VLM 中:
   - 被 DROP_CANDIDATE 标记: 71 条中有部分已播放(6条)
   - 仿真结束时仍在队列中: 部分
   - 被队列 eviction (FIFO pop): 部分
   - 过期 (expired): 部分

DROP_CANDIDATE 标记统计:
------------------
  marked: 71 条 (77% of all VLM)
  其中 played: 6 条 (8% of marked)
  其中 unplayed: 65 条 (92% of marked)

  标记规则: semantic=LOW + queue near full
  标记不终止生命周期，不执行实际丢弃。
  92% 的被标记条目最终未播放，说明标记具有预测性。
