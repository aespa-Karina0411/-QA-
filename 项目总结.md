# 边缘视觉语音问答系统 — 项目工程总结

## 一、项目初始状态

系统起源于一个线性原型：摄像头 → YOLO → TTS 播放。所有逻辑混杂在 `main.py` 的 while 循环中，每个模块直接调用下一个模块，无状态记忆，无调度控制。

**核心问题**：

1. **模块耦合严重**：YOLO 输出直接喂给 TTS，无中间语义层。decision、spatial、speech 代码交织在同一文件中
2. **无状态管理**：每帧重新理解世界，上一帧的"前方有行人"与下一帧"前方有行人"无法建立关联，导致每帧重复播报
3. **同步阻塞**：VLM 云端调用阻塞主线程，画面冻结，ASR 无法响应
4. **多源冲突**：导航播报、VLM 回答、系统提示争先调用 TTS，无优先级控制，语音互相覆盖
5. **无验证体系**：行为正确性全靠人工感受，无法量化"是否重复播报""是否漏报""是否抖动"

---

## 二、核心架构重构

### 1. Context 状态机

**问题**：系统无法"记住世界"。同一物体连续存在 10 帧，每帧都被当作新物体重复播报。用户移动导致场景切换后，之前的对话历史仍然保留，VLM 在旧场景背景下回答新问题。

**解决**：建立三层 context 结构（`scene` / `dialog` / `system`），使系统从"每帧重新理解世界"变为"持续追踪世界状态"。

- `scene` 层：存储当前环境结构化数据 + 场景版本号 + 稳定状态标记
- `dialog` 层：存储最近 6 轮对话上下文，10 秒无交互自动清空
- `system` 层：存储模式、静音状态、事件日志

场景变化通过 `is_scene_stable` 检测：连续 3 帧 `(class_zh, direction)` 集合一致才判定稳定。场景切换时自动清空对话历史。VLM 通过 `build_vlm_context` 白名单获取 scene objects + dialog history，不再接收完整 context。

### 2. Decision 行为层

**问题**：原有决策逻辑边判断边生成文本，且无抑制机制，导致频繁重复播报。

**解决**：将决策拆分为 5 层：

| 层 | 职责 | 位置 |
|----|------|------|
| State | tracker 字典记录每个 `(class_zh, direction)` 的 history/first_seen/last_dist | `self.trackers` |
| Behavior | 基于距离趋势、危险标记、持续时长判定 intent（`ENVIRONMENT_DESC`/`STATUS_UPDATE`/`APPROACHING`/`EMERGENCY_WARNING`） | `get_decision` for 循环 |
| Suppression | `repeat_interval`（3s 同物体限频）+ `INTENT_LEVEL` 等级比较（只允许升级，阻止降级/同级重复） | 入队前检查 |
| Selection | `_score` 加权函数：紧急 100 + 危险 30 + 很近 20 + 靠近 15，`max()` 多选一 | 频率控制前 |
| Expression | 独立 `ExpressionEngine` 类，模板库 + 随机选择 + 风格后处理，延迟到 Controller 侧生成 | `expression/` 模块 |

### 3. Spatial 平滑与迟滞

**问题**：YOLO bbox 面积单帧波动导致 `get_distance_label()` 在"较近/很近"之间反复跳变，触发 decision 误报 approaching。

**解决**：在 `parse_environment()` 末尾增加后处理：

- **5 帧多数投票**：`smooth_distance(history)` → `max(set, key=count)`
- **迟滞规则**："较近→很近"需连续 2 帧确认；"很近→较近"需连续 3 帧确认（防止过快退出危险状态）
- **过期清理**：物体消失后自动清除其历史缓存

### 4. Speech 系统重构

**问题**：每次播报 `new Thread().start()`，高并发下线程爆炸；无队列机制，后续消息覆盖前序。

**解决**：两级 PriorityQueue 架构：

- **消息队列**（`self.queue`）：`update()`/`speak_now()` 入队，`_run_consumer` 每 0.5s drain，1s 窗口内同 source 消息合并（`"，".join()`）
- **播放队列**（`_play_queue`）：唯一 `_player_loop` daemon 线程串行消费，`interrupt` 消息 priority=0 跳队打断当前播放
- TTS 通过 `TTSBackend` 抽象接口注入，`LocalTTS` 包装 Piper，`stop()` 统一走抽象层

### 5. VLM 调度系统

**问题**：原 `ask_async` 使用 `self.busy` 锁，请求正在处理时新请求直接被丢弃。callback 直接调用 `speak_now`，绕过仲裁。

**解决**：引入 `deque` 请求队列 + `result_queue` 结果队列：

- `ask_async` 入队（限长 2），无 callback
- `_scheduler_loop` daemon 线程，3s 冷却弹出一个任务
- `_run_task` 构建 messages → 调用 cloud_adapter → 结果入 `result_queue`（附带 `version`）
- Controller 每帧 `poll_result()` + `_poll_vlm_results()`，`_on_vlm_result` 校验 scene version（过期丢弃）

### 6. Speech Arbitration（语音仲裁层）

**问题**：navigation、VLM 回答、系统提示三源竞争 TTS，无优先级控制。

**解决**：`SpeechArbitrator` 统一入口：

- `submit(item)`：VLM 5s 过期丢弃
- `select_next()`：按 priority 升序弹出（1=紧急 > 2=VLM > 3=普通 nav）
- `can_play_vlm()`：距上次 decision ≥ 1s 才允许 VLM 输出
- 主循环每帧 `_drain_arbitrator()`：仅在 `not is_speaking()` 时发放

### 7. VLM Intent Layer

**问题**：用户问"那是什么"，VLM 收到原始文本 + 环境描述，无法确定指代对象。

**解决**：`parse_vlm_intent()` 关键词匹配 → 结构化查询：
- "那是什么" → `object_query`，自动选取前方最近物体
- "是什么颜色" → `detail_query`，注入物体类别和位置
- "周围有什么" → `scene_query`，不指定 target

`build_prompt()` 生成包含 `class_zh / direction / distance` 的结构化 prompt，替代原始文本。

---

## 三、系统验证体系

建立 `simulate_log → log_analyzer → run_validation` 三件套，使系统行为可量化验证：

| 检测器 | 方法 | PASS 条件 |
|--------|------|-----------|
| 重复播报 | 连续两次 played 且 text 相同 | 0 |
| 漏报 | 同物体距离从 较远→很近 但未播报 | 0 |
| 抖动 | 同物体 distance 变化次数 > 帧数 50% | < 2 |
| 语音频率 | played 事件平均间隔 | 1.5s ~ 5s |
| VLM 打断 | VLM played 距上次 decision < 3s | 0 |
| VLM 饥饿 | queued=True 但从未 played | 0 |
| 乱序插入 | VLM 与 decision 在同一播放窗口交替 | 0 |

所有检测器基于 `run_log.json` 运行，无需真实摄像头。

---

## 四、最终系统能力

- 不重复播报：`last_intent` 等级比较 + `repeat_interval` 双重抑制
- 不漏报关键事件：`INTENT_LEVEL` 确保 upgrade 不被抑制
- 距离不抖动：5 帧多数投票 + 迟滞规则
- VLM 不打断主系统：`Arbitrator.can_play_vlm()` 空闲窗口检测 + `select_next()` 优先级队列
- VLM 不过期回答：`submit()` 5s 超时丢弃 + callback 层 scene version 校验
- 具备基本理解能力：`parse_vlm_intent` 关键词解析 + 结构化 prompt 构造

---

## 五、第五阶段：工业级多队列语音仲裁系统

### 5.1 单队列瓶颈

SpeechArbitrator 原单一 FIFO 队列 + priority 排序架构在极端负载下暴露致命缺陷：

- **WARNING 被丢弃**：34 次（73.9%），t=0.6s 首次丢失 — 安全关键路径失守
- **顺序违规**：6 次 order_violation — 优先级调度失效
- **VLM 完全饥饿**：98.6% VLM 未播放 — 用户交互不可用
- **首次崩溃**：t=0.4s — 系统无任何保护余量

### 5.2 三队列调度器

重构为三个独立队列 + 加权轮询调度器：

| 队列 | 容量 | 策略 |
|------|------|------|
| WARNING_QUEUE | max=3 | 永不丢弃，满则替换最旧 WARNING |
| VLM_QUEUE | max=5 | LIFO 取最新（FIFO 丢旧），保留最新提问 |
| ENV_QUEUE | max=3 | 满则拒新（直接丢弃） |

调度器优先级链：`VLM 保活(>4s) → 硬性交错(每 2 WARNING → 1 VLM) → WARNING 优先 → ENV 降级 → 加权轮询[VLM,ENV,ENV] → 兜底`

### 5.3 节流门控（Throttle Guard）

1.5s 最小播放间隔：WARNING 始终放行，VLM 延迟一次 requeue，ENV 直接丢弃。VLM 保活路径和硬性交错路径绕过节流（防止 VLM 被节流误杀）。

### 5.4 验证结果

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| WARNING 丢弃 | 34（73.9%） | **0** ✅ |
| 顺序违规 | 6 | **0** ✅ |
| 首次崩溃 | t=0.4s | **永不发生** |
| VLM 已播放 | 15 | **27**（+80%） |
| VLM 饥饿率 | 98.6% | 95.9% |

---

## 六、第六阶段：架构完善

### 6.1 OutputPolicy 输出策略层

新增 `perception/output_policy.py`，在 Decision 输出 → Arbitrator 之间插入策略层：

- **WARNING 永远放行**：安全关键路径绝对保障
- **Speech Budget**：5s 窗口内最多 2 条语音，防止连续播报轰炸
- **ENV 降噪**：相同 objects 集合不重复播报
- **可回滚开关**：`enable_output_policy = True/False`

### 6.2 VLM 多模态一致性修复

**关键修复**（`vlm_manager.py:_run_task`）：当 dialog history 非空时，VLM 消息中丢失了 image_url。第二轮起 VLM 变为纯文本模型。统一消息构建后，每轮请求均携带 `image_url`。

VLM 请求队列改为"只保留最新"（新请求到达时清除旧请求），延迟从 20s 降至 1~3s。超时窗口从 5s 放宽至 8s，减少误杀 40%。

### 6.3 Cold Start 冷启动机制

系统启动后 2s 窗口内首次环境播报自动绕过 throttle 和 OutputPolicy：

```
启动语句播放 → Scene 首次稳定 → Cold Start env broadcast (is_cold_start_env=True)
                                 → bypass_throttle + bypass_policy
                                 → 后续正常调度
```

### 6.4 Decision 层 ENV 降噪

`decision_utils.py:get_decision` 中 ENVIRONMENT_DESC 意图增加抑制条件：

```python
if not obj.is_danger and distance == "较远":
    intent = None  # 抑制
```

直接减少 ~60% ENV 噪声。

### 6.5 全链路追踪诊断

在所有语音路径插入 trace_id 追踪：

```
SUBMIT → ARBITRATOR_IN → (DROP) → SELECT → PLAY → SPEAK_START → SPEAK_END
```

覆盖 Controller、Arbitrator、SpeechManager 三个模块，异常时可快速定位丢失环节。

---

## 七、极限压力测试（Break-the-System Test）

### 7.1 测试设计

构造 304 条极端负载事件（120 秒，6 层叠加）：

| 负载 | 参数 |
|------|------|
| Decision 持续洪水 | 每 0.8~1.2s 一条，20% WARNING |
| VLM 高频请求 | 每 3s 一次，4 档延迟分布 |
| Spike 冲击 | 每 20s 一次，2s 内 15~25 条混合事件 |
| 调度冲突 | WARNING + VLM + ENV 同时触发 |
| 队列溢出 | 快速 burst 覆盖 queue 上限 |

### 7.2 崩溃时间线对比

| 事件 | 优化前 | 优化后 |
|------|--------|--------|
| 首次 overflow | t=0.4s | 保护机制生效 |
| 首次 WARNING 丢弃 | t=0.6s (CRITICAL) | 完全消除 |
| 首次 VLM 超时 | t=37.4s | t=37.4s |
| 最差窗口丢弃 | 36 条/10s | 27 条/10s |

### 7.3 真实链路验证

构建 `tests/test_real_pipeline.py` 通过真实 Controller 注入 60s 事件流：

| 验证项 | 结果 |
|--------|------|
| 启动语句 | 100% 播放 ✅ |
| VLM 10 次提问 | 10/10 播放 ✅ |
| Cold Start 环境播报 | 仅 1 次 ✅ |
| 启动期导航拦截 | `is_startup_phase` 生效 ✅ |

---

## 八、当前系统架构

```
Camera → YOLO                   ASR → IntentParser
    ↓                                ↓
    └──── Controller.handle_event ────┘
                ↓
      ┌─────────┼─────────┐
      │  navigation │ user_input │
      └──────┬─────┴─────┬───┘
             │           │
      SpatialParser  IntentParser
             │           │
      DecisionMaker  ┌──┴──────────────┐
             │       │ VLM IntentParser │
      ExpressionEngine │ VLMManager(VLM)│
             │       └──────┬──────────┘
             │              │
      ┌──────┴──────┐      │
      │ OutputPolicy│      │
      └──────┬──────┘      │
             │              │
      ┌──────┴──────────────┴──┐
      │   SpeechArbitrator      │
      │   ┌────┐ ┌────┐ ┌───┐   │
      │   │WARN│ │VLM│ │ENV│   │
      │   └─┬──┘ └─┬──┘ └─┬─┘   │
      │     └──┬────┴──┬──┘     │
      │      Scheduler │        │
      └──────────┬─────┘───────┘
                 ↓
          SpeechManager
          (2-PQ + drain)
                 ↓
            TTSBackend
```

---

## 九、存在的不足

1. **VLM 延迟**：Mock 环境 0.3s，DashScope API 生产环境首包 1~3s
2. **单帧决策**：DecisionMaker 无时间窗口聚合，可能遗漏快速累积的小变化
3. **ENV 降噪有限**：`_is_significant` 比较 objects 集合，无法识别 "同一物体改变方向"
4. **距离估计粗糙**：bbox 面积比例法，侧对摄像头时误差大
5. **边缘设备未部署**：仅 Windows 验证，未在树莓派运行
6. **无纯文本 fallback**：VLM 不可用时仅返回固定错误提示，无 LLM 基本问答能力
7. **模拟器差异**：`simulate_log.py` 独立仲裁器与真实 `SpeechArbitrator` 需持续对齐
