# 项目日志：ImageQAVoice 智能语音问答助手

## 一、项目概述

**目标**：搭建面向视障人士的实时语音问答系统。用户通过摄像头获取环境图像，语音提问，系统利用云端多模态大模型理解图像内容，以语音返回答案。

**技术路线**：端侧（摄像头 + YOLO 物体检测）→ 云端（多模态大模型理解）+ 端侧（Piper 离线 TTS 合成）→ 语音输出。

---

## 二、第一阶段：功能原型验证（VLM + TTS + ASR 联调）

### 2.1 核心模块集成

依次完成以下模块的独立验证和串联测试：

| 模块 | 技术选型 | 验证结果 |
|------|---------|----------|
| 多模态大模型 | Qwen-VL（DashScope API, `qwen3.5-flash`） | 三张测试图均正确描述场景、定位物体、回答推理问题 |
| 文本转语音 | cosyvoice-v1（云端）/ Piper（本地） | 生成 mp3 流畅播放，首包延迟 < 800ms |
| 语音识别 | fun-asr-realtime（DashScope）/ Vosk（本地） | PTT 按键录音，实时返回文本 |

### 2.2 全流程串联

**版本演进**：`test-total-ver0.1`（图片 → VLM → TTS 单次）→ `ver0.4`（ASR 语音输入替代文本框）→ 代码模块化拆分 ASR / VLM / TTS 为独立模块。

**多轮对话**：维护独立 `messages` 列表实现单图片内上下文记忆，支持按键控制切换图片。

---

## 三、第二阶段：实时环境感知（YOLO + Spatial + Decision）

### 3.1 差距分析

静态图片原型无法满足视障用户实时行走需求。核心缺失：实时视频采集、边缘端物体检测、端云协同策略、离线模式。

### 3.2 基础设施搭建

- **摄像头模块**（`camera_utils.py`）：OpenCV VideoCapture 封装，支持 Base64 编码供 VLM 消费
- **YOLO 物体检测**（`yolo_utils.py`）：YOLOv8n 模型，每 2 帧检测一次（`imgsz=320` 降低计算量）
- **PTT 语音输入**（`asr_utils.py`）：`keyboard` 库实时按键检测，按住说话/松开识别
- **TTS 异步播放**（`tts_utils.py`）：`pygame.mixer` 异步 + 打断机制，不阻塞主循环
- **容错机制**：`@retry` 指数退避重试（2s/4s/8s）、OpenAI 客户端 20s 超时、资源清理完整性

### 3.3 实时路况播报模式

**问题**：YOLO 检测结果直接播报会导致每帧重复、多个物体冲突、静止物体啰嗦。

**方案**：建立完整感知-语义-决策链路。

**产出模块**：

| 模块 | 职责 | 关键技术 |
|------|------|----------|
| `spatial_utils.py` | YOLO bbox → 结构化语义 | 方向（x_center/width 三分法）、距离（面积比阈值）、自动合并同类同方向 |
| `decision_utils.py` | 结构化数据 → 播报决策 | tracker 状态记忆、趋势分析（approaching/staying）、优先级排序、播报抑制 |
| `tts_local_utils.py` | 决策文本 → 语音输出 | Piper 离线中文 TTS、pygame 异步 + 打断 |

**测试结果**：行人靠近场景正确输出"左侧行人较远"→"左侧行人很近正在靠近"；汽车驶过场景紧急播报"前方汽车很近正在靠近"；静止物体仅播报一次"持续存在"后沉默。

---

## 四、第三阶段：系统架构重构（事件驱动 + 分层设计）

### 4.1 问题诊断

原型阶段所有逻辑混杂在 `main.py` 的 while 循环中，模块直接相互调用：
- Controller 过重（业务逻辑 + 调度 + 状态管理混合）
- 无统一事件入口（YOLO/ASR 各自触发，路径不一致）
- 无模式管理（导航播报与用户问答互相覆盖）
- 无状态记忆（每帧重新理解世界）

### 4.2 重构方案

**引入 Controller 作为唯一调度中心**：
- 所有输入（视觉/语音）统一为 Event 格式（`type + data + timestamp`）
- navigation（环境感知）与 user_input（语音交互）两类事件统一处理
- 模块间不再直接调用，全部经 Controller 路由

**完成系统分层**：

```
感知层(YOLO) → 语义层(spatial_utils) → 决策层(decision_utils) → 表达层(ExpressionEngine) → 输出层(SpeechManager)
```

**关键产出**：
- `IntentParser`：关键词意图分类（mute/resume/describe/scene_qa/general_qa）
- `EnvironmentDescriber`：结构化环境 → 自然语言描述
- `local_scene_qa`：基于当前环境的即时问答（降低云端依赖）
- Controller 模式管理：navigation（自动播报）/ assistant（用户交互），含 idle_timeout 自动切换

### 4.3 VLMManager"越权"修复

**问题**：VLMManager 内部直接调用 SpeechManager，绕过 Controller 调度。

**修复**：VLMManager 退化为纯异步计算层，通过 callback 回传结果；Controller 在 `_on_vlm_result` 统一处理语音输出。

---

## 五、第四阶段：系统深度优化

### 5.1 Context 生命周期管理

对话上下文自动清理机制。`_should_reset_dialog` 10 秒无输入自动清空 `dialog_history`。

### 5.2 Context 状态机

**目标**：从"存数据"→"管理状态"，让系统"记住世界"。

三层结构 `scene` / `dialog` / `system`：
- `scene` 层：环境结构化数据 + 版本号 + 稳定状态标记
- `dialog` 层：最近 6 轮对话上下文
- `system` 层：模式、静音状态、事件日志

场景变化检测：`is_scene_stable` 连续 3 帧确认。VLM context 白名单：`build_vlm_context` 仅暴露 scene objects + dialog history[-3:]。
✔ 换场景不记错 ✔ VLM 不胡说 ✔ 对话不串

### 5.3 Speech 系统重构（两级 PriorityQueue）

**目标**："碎片播报"→"连续表达"。

- 消息队列（`self.queue`）：`update()`/`speak_now()` 入队，`_run_consumer` 每 0.5s drain，同 source 合并
- 播放队列（`_play_queue`）：唯一 `_player_loop` daemon 线程串行消费，永不并发
- 分级播报：interrupt 消息 priority=0 跳队打断，普通消息排队合并
✔ 不阻塞主循环 ✔ 不语音叠加 ✔ 线程数恒定为 2

### 5.4 Decision 行为控制化

**5 层分离**：
1. State（`trackers` 字典）：每个 `(class_zh, direction)` 独立追踪 history/first_seen/last_dist
2. Behavior（4 种 intent）：`ENVIRONMENT_DESC` / `STATUS_UPDATE` / `APPROACHING` / `EMERGENCY_WARNING`
3. Suppression（双重）：`repeat_interval` 3s 限频 + `INTENT_LEVEL` 等级比较（只允许升级）
4. Selection（`_score` 加权）：紧急 100 + 危险 30 + 很近 20 + 靠近 15，`max()` 单选
5. Expression（延迟生成）：不在 Decision 内部生成文本
✔ 不重复说话 ✔ 有优先级 ✔ 会沉默 ✔ 单输出

### 5.5 Spatial 平滑与迟滞

**问题**：YOLO bbox 面积单帧波动导致 distance 在"较近/很近"间来回跳。

**方案**：5 帧多数投票 `smooth_distance()` + 迟滞规则（"较近→很近"需 2 帧确认，"很近→较近"需 3 帧确认）。物体消失自动清理缓存。

### 5.6 Expression 层独立

**目标**：文本生成从 Decision 完全剥离。

`expression/` 模块三个文件：
- `templates.py`：intent → `list[str]` 模板池
- `style.py`：`STYLE_RULES` 按 intent 分派（紧急 ✚⚠️+！，普通 ✚。）
- `expression_engine.py`：`ExpressionEngine.generate(decision)` → `random.choice(templates).format()`

### 5.7 VLM 调度增强

**问题**：原 `ask_async` 用 `self.busy` 锁，请求在途时新请求直接丢弃。callback 直接 speak 绕过仲裁。

**方案**：
- 删除 `busy` 锁 → `deque` 请求队列（限长 2）+ `result_queue`
- `_scheduler_loop` daemon 线程，3s 冷却弹出一个任务
- Controller 每帧 `poll_result()` + version 校验
✔ 连续提问不丢请求 ✔ YOLO 不掉帧 ✔ VLM 慢时系统仍流畅

### 5.8 Speech Arbitration（语音仲裁）

**问题**：navigation / VLM / system 三源竞争 TTS，无优先级控制。

**方案**：`SpeechArbitrator` 统一入口。
- `submit(item)`：VLM 5s 过期丢弃
- `select_next()`：priority 1(紧急)>2(VLM)>3(普通导航)
- `can_play_vlm()`：距上次 decision >= 1s 空闲窗口
- 主循环每帧 `_drain_arbitrator()`：仅在 `not is_speaking()` 时发放

### 5.9 VLM Intent Layer

**问题**：用户问"那是什么"，VLM 收到原始文本无法确定指代对象。

**方案**：`parse_vlm_intent()` 关键词匹配 → 结构化查询。
- "那是什么" → `object_query`，自动选取前方最近物体
- "是什么颜色" → `detail_query`，注入 class_zh/direction/distance
- "周围有什么" → `scene_query`
- `build_prompt()` 生成包含物体类别、位置、距离的结构化 prompt

### 5.10 系统验证体系

**目标**：从"人工感受"变为"量化验证"，无需真实摄像头。

**三件套**：
- `simulate_log.py`：9 个场景生成可控测试数据
- `log_analyzer.py`：7 项检测器自动分析
- `run_validation.py`：一键运行 → PASS/FAIL

**检测指标**：

| 检测器 | 方法 | PASS 条件 |
|--------|------|-----------|
| 重复播报 | 连续两次 played 且 text 相同 | 0 |
| 漏报 | 同物体距离 较远→很近 但未播报 | 0 |
| 抖动 | 同物体 distance 变化次数 > 帧数 50% | < 2 |
| 语音频率 | played 事件平均间隔 | 1.5s ~ 5s |
| VLM 打断 | VLM played 距上次 decision < 3s | 0 |
| VLM 饥饿 | queued=True 但从未 played | 0 |
| 乱序插入 | VLM 与 decision 在同一播放窗口交替 | 0 |

---

## 六、系统架构总览

```
YOLO → SpatialParser → Context(scene) → DecisionMaker(intent+obj) → ExpressionEngine(text)
                                                                          ↓
ASR → IntentParser → [local_scene_qa / VLM_IntentParser → VLMManager] → SpeechArbitrator
                                                                          ↓
                                                                     SpeechManager
                                                                          ↓
                                                                      TTSBackend (Piper/DashScope)
```

**关键设计原则**：
- Controller 是唯一调度入口（事件驱动）
- Context 是唯一状态源（scene/dialog/system 三层）
- SpeechArbitrator 是唯一语音出口（优先级队列）
- VLMManager 退化为纯异步计算层（poll_result 解耦）

---

## 八、第五阶段：工业级多队列语音仲裁系统

### 8.1 单队列瓶颈分析

SpeechArbitrator 原实现采用单一 FIFO 队列 + priority 排序。极限压测暴露 3 个致命问题：

| 问题 | 表现 | 严重程度 |
|------|------|----------|
| WARNING 被丢弃 | 34 次（73.9%），t=0.6s 首次丢失 | CRITICAL 安全失效 |
| 优先级乱序 | 6 次 order_violation | 调度失效 |
| VLM 100% 饥饿 | 98.6% VLM 未播放 | 系统不可用 |

### 8.2 三队列调度器架构

将单队列重构为三队列独立调度：

```
WARNING_QUEUE (max=3, 永不丢弃, 满则替换最旧)
VLM_QUEUE (max=5, LIFO 取最新, FIFO 丢旧)
ENV_QUEUE (max=3, 满则拒新)
```

调度器 6 层优先级链：

```
1. VLM 保活 (>4s 未播 → 强制)
2. 硬性交错 (每 2 WARNING → 1 VLM)
3. WARNING 优先
4. ENV 降级 (vlm > 3 时暂停 ENV)
5. 加权轮询 [VLM, ENV, ENV]
6. 兜底
```

### 8.3 节流门控（Throttle Guard）

在调度器出口增加 1.5s 最小间隔门控：

| 优先级 | 行为 |
|--------|------|
| WARNING | 始终放行 |
| VLM | 延迟一次 (requeue at front)，二次超节流丢弃 |
| ENV | 直接丢弃 |

### 8.4 验证结果

优化前后对比（同一 304 条目极端负载）：

| 指标 | 优化前 | 优化后 | 变化 |
|------|--------|--------|------|
| WARNING 丢弃 | 34（73.9%） | 0 | ✅ 完全消除 |
| 顺序违规 | 6 | 0 | ✅ 完全消除 |
| 首次崩溃 | t=0.4s | 永不崩溃 | ✅ WARNING 永不溢出 |
| VLM 已播放 | 15 | 27 | ▲ +80% |
| VLM 饥饿率 | 98.6% | 95.9% | ▼ -2.7pp |

---

## 九、第六阶段：架构完善与策略层

### 9.1 OutputPolicy 输出策略层

新增 `perception/output_policy.py`，在 Decision → Arbitrator 之间插入策略层：

| 规则 | 效果 |
|------|------|
| WARNING 永远放行 | 保障安全关键路径 |
| Speech Budget | 5s 窗口内最多 2 条 |
| ENV 降噪 | 相同 objects 不重复播 |

`enable_output_policy` 开关控制，可回滚。

### 9.2 VLM 多模态一致性修复

**问题**（`vlm_manager.py:_run_task`）：首次 VLM 调用包含图像，后续轮次因 dialog history 非空跳过了图像拼接。VLM 在第二轮起变成纯文本模型。

**修复**：统一消息构建，当前问题始终携带 `image_url`：

```python
messages = [system_msg, *history_msgs, {"role": "user", "content": [
    {"type": "image_url", "image_url": {"url": image}},
    {"type": "text", "text": text},
]}]
```

VLM 请求队列改为"只保留最新"（`self.queue.clear()` 旧请求全部丢弃），将延迟从 20s 降至 1~3s。

### 9.3 Cold Start 冷启动

系统启动后 2s 窗口内触发的首次环境播报自动绕过 throttle/policy：

```
STARTUP → 播报
    ↓ Scene 稳定
COLD_START → env broadcast (bypass throttle + policy)
    ↓ 窗口过期
正常调度 (throttle + policy 生效)
```

### 9.4 ENV 决策层降噪

`decision_utils.py` 新增：ENVIRONMENT_DESC 意图下，非危险物体 + 距离较远 → 抑制不生成。直接减少 ~60% 噪声。

---

## 十、第七阶段：极限压测与 Break-the-System

### 10.1 压测设计

构造 304 条事件（6 层叠加，120s）：

| 负载层 | 参数 |
|--------|------|
| Decision 持续洪水 | 每 0.8~1.2s 一条，20% WARNING |
| VLM 高频请求 | 每 3s 一次，4 档延迟分布（0.3~8s） |
| Spike 冲击 | 每 20s 一次，2s 内 15~25 条混合事件 |
| 背压场景 | 触发 rejected_backpressure |
| 调度冲突 | WARNING + VLM + ENV 同时触发 |
| 队列溢出 | 快速 burst 覆盖 queue 上限 |

### 10.2 崩溃时间线

| 事件 | 优化前 | 优化后 |
|------|--------|--------|
| 首次 overflow | t=0.4s | 保护机制生效，WARNING 不放溢出 |
| 首次 WARNING 丢弃 | t=0.6s | 完全消除 |
| 首次 VLM 超时 | t=37.4s | t=37.4s（不变） |
| 最差窗口丢弃 | 36 条/10s | 27 条/10s |

### 10.3 真实链路验证

构建 `tests/test_real_pipeline.py`：

- 实例化真实 Controller + Arbitrator + SpeechManager + VLMManager + DecisionMaker
- 注入 60s 事件流（navigation + user_query + warning）
- 全链路 trace_id 追踪

结果：

| 项目 | 结果 |
|------|------|
| 启动语句 | 100% 播放 ✅ |
| VLM 10 次提问 | 10/10 播放 ✅ |
| 启动期导航拦截 | `is_startup_phase` 生效 ✅ |
| Cold Start 环境播报 | 仅一次 ✅ |

---

## 十一、当前系统架构

```
Camera → YOLO                   ASR → IntentParser
    ↓                                ↓
    └──── Controller.handle_event ────┘
                ↓
      ┌─────────┼─────────┐
      │  navigation │ user_input │
      └──────┬─────┴─────┬───┘
             │           │
      SpatialParser  IntentParser
             │           │
      DecisionMaker  ┌──┴──────────────┐
             │       │ VLM IntentParser │
      ExpressionEngine │ VLMManager(VLM)│
             │       └──────┬──────────┘
             │              │
      ┌──────┴──────┐      │
      │ OutputPolicy│      │
      └──────┬──────┘      │
             │              │
      ┌──────┴──────────────┴──┐
      │   SpeechArbitrator      │
      │   ┌────┐ ┌────┐ ┌───┐   │
      │   │WARN│ │VLM│ │ENV│   │
      │   └─┬──┘ └─┬──┘ └─┬─┘   │
      │     └──┬────┴──┬──┘     │
      │      Scheduler │        │
      └──────────┬─────┘───────┘
                 ↓
          SpeechManager
          (2-PQ + drain)
                 ↓
            TTSBackend
```

---

## 十二、存在不足与下一步计划

1. **VLM 延迟**：模拟环境使用 Mock VLM（0.3s），生产环境 DashScope API 首包 1-3s
2. **单帧决策**：DecisionMaker 在每次 scene 稳定后单次决策，无时间窗口聚合，可能遗漏快速累积的小变化
3. **ENV 降噪仍不完善**：`_is_significant` 仅比较 objects 集合，无法区分"同一个物体改变方向"
4. **距离估计粗糙**：bbox 面积比例，物体侧对摄像头时误差大
5. **边缘设备未部署**：当前仅 Windows 验证，未在树莓派运行
6. **无纯文本 fallback**：VLM 不可用时无法提供基本问答
7. **模拟与真实差异**：`simulate_log.py` 独立仲裁器与真实 `SpeechArbitrator` 行为不完全一致，需持续对齐

---

## 十三、第六阶段：智能响应路由（Phase 1 交互修复）

### 13.1 对话模式注入

用户提问后激活 3s 对话优先级窗口，屏蔽 ENVIRONMENT_DESC/STATUS_UPDATE 播报，EMERGENCY_WARNING 始终放行。4 行修改仅限 `controller.py:_route_speech_decision`。

### 13.2 VLM 多模态一致性修复

**问题**：`vlm_manager.py:_run_task` 中当 dialog history 非空时丢失了 image_url。第二轮起 VLM 变成纯文本模型。

**修复**：统一消息构建结构，每轮请求始终携带 `image_url`。VLM 请求队列改为"只保留最新"（新请求到达时清除旧请求）。

### 13.3 ResponseRouter 智能路由

新增 `core/response_router.py`，将用户意图路由到最合适模块：

- `ENV_QUERY`（"室内还是室外？"）→ 基于当前 YOLO objects 直接判断，不走 VLM
- `OBJECT_QUERY`（"有没有人？""汽车在哪里？"）→ 从 scene objects 直接查找回答
- `GENERAL_QA`（"戴眼镜吗？"）→ 强制走 VLM
- IntentParser 新增 slots 提取（`has_object` 等）

### 13.4 Speech Throttle + VLM Fallback

- 节流门控：WARNING 始终放行，VLM 延迟一次 requeue，ENV 直接丢弃
- VLM fallback 文本：`"我暂时无法判断，可以换个角度再试一下"`，绝不回退到 Decision 输出

---

## 十四、Phase A：USER_FOCUS 行为修复

### 14.1 问题发现

真实链路追踪发现：VLM 应答被 `_on_vlm_result` 中的 `version != scene.version` 版本检查丢弃。触发条件：USER_FOCUS 期间 `_on_navigation_event` 仍在更新 `scene.version`（只屏蔽了 speech，未屏蔽 scene 状态变更）。

### 14.2 修复

`_on_navigation_event` 增加 `user_focus["active"]` 检查：USER_FOCUS 期间直接返回，不更新 scene 状态和 version。

### 14.3 效果

VLM 完整链路恢复：
```
USER_QUERY → VLM_TRIGGER → VLM_RESULT_READY → VLM_RESULT_CALLBACK
→ VLM_ENQUEUE → SELECT → PLAY → SPEAK_START → SPEAK_END
```

---

## 十五、Phase B：speech_lock 一致性锁

### 15.1 设计

锁仅存在于 `SpeechManager` 内部，Controller 零锁感知：

- `try_play(item)` 为唯一播放入口
- WARNING 可打断任意播放（`priority ≤ 1 → lock ignored`）
- VLM 原子播放（`can_interrupt = False`，播放期间不被非 WARNING 打断）

### 15.2 效果

| 当前播放 | 新请求 | 结果 |
|----------|--------|------|
| 任意 | WARNING | 打断 ✅ |
| VLM | 非 WARNING | 阻塞 `[LOCK BLOCK]` |
| ENV | 任意 | 允许 |

---

## 十六、Phase D：VLM 调度保障体系（Step 1-3）

### 16.1 Step 1：USER_FOCUS 保留槽

USER_FOCUS 期间 VLM 不进入三队列竞争，存入 `self.vlm_reserved_slot`。`select_next()` 第一时间检查保留槽 → 直接返回，不经过调度器。

### 16.2 Step 2：VLM 老化提升（Aging Boost）

`select_next()` 中在正常调度前检查 vlm_queue：若任何条目等待时间 > 4s，强制提升其优先级，绕过正常调度链直接播放。设置 `force_play=True` 确保绕过 throttle。

### 16.3 Step 3：VLM 评分策略层

`_pop_vlm()` 从 LIFO pop 改为评分选择（遍历全队列，最高分取出）：

```python
score = base(10/30) + wait_time × 10
```

force_play 条目独立优先（`[VLM_FORCE_SELECT]`），不参与评分竞争。wait_time 主导选择，无饱和。

---

## 十七、Phase E0：DROP_CANDIDATE 观测与 Pi 模拟

### 17.1 DROP_CANDIDATE 标记

在 `SpeechArbitrator.submit()` 中新增观测标记：VLM 条目在 queue 接近满时被标记为 `drop_candidate=True`，打印 `[TRACE][DROP_CANDIDATE]`，但**不执行实际丢弃**（纯观测）。

### 17.2 PC 伪树莓派负载模拟

通过 config 注入 Pi 环境约束：

| 参数 | 作用 |
|------|------|
| `SIMULATE_PI = True` | 开启模拟 |
| `VLM_DELAY = 0.3s` | 注入推理延迟 |
| `MAX_VLM_QUEUE = 3` | 压缩队列容量 |
| `SIMULATION_MODE = "stress"` | 高频 + burst 模式 |

### 17.3 分布验证

normal 模式：30 次 DROP_CANDIDATE。stress 模式：91 次（3x）。100% 标记在 LOW-semantic VLM、queue_size=max-1 的临界区，规则精准无误标。

---

## 十八、Phase C：工业级配置系统

### 18.1 配置架构

```
config/
├── config.yaml        # 基础配置
├── config_pc.yaml     # PC 环境覆盖（最小差异）
├── config_pi.yaml     # 树莓派覆盖（最小差异）
core/
├── config_loader.py   # YAML + deep merge + get("a.b.c")
├── global_config.py   # 全局 CONFIG 单例
```

### 18.2 Profile 切换

优先级链：`显式传参 → EDGE_VISION_PROFILE 环境变量 → config.yaml 的 profile 键 → 自动检测（Linux → "pi"，其他 → "pc"）`。Linux 环境若检测到 pc profile 会打印 WARN 提示。

### 18.3 接入模块

| 模块 | 接入参数 |
|------|----------|
| `controller.py` | `decision_interval`、`fps_limit`、`enable_env`、`enable_vlm` |
| `vlm_manager.py` | `scheduler_interval`、`max_queue`、`simulate_delay`、`drop_when_full` |
| `speech_arbitrator.py` | 全 10 项硬编码 → `CONFIG.get()`，含队列容量、超时、间隔、阈值 |
| `config.py` | 兼容层：`VLM_MODEL`、`TTS_MODEL` 等从 CONFIG 读取 |

### 18.4 审计结果

22/22 项 Phase C Step 1A FINAL CHECK 通过：加载顺序、profile 优先级、deep merge、dot-notation、单例无副作用。

---

## 十九、统一验证框架

### 19.1 结构

```
tests/
├── validation/
│   ├── result_schema.py        # 统一 ValidationResult 结构
│   ├── validator_core.py       # 验证核心调度器
│   ├── phaseA_user_focus.py    # USER_FOCUS 行为分析
│   ├── phaseB_speech_lock.py   # speech_lock 验证
│   └── phaseA_path_integrity.py
├── scenarios/
│   └── user_focus_scenario.py  # 真实 Controller 驱动场景
├── tools/
│   └── diagnose_trace.py
├── run_validation.py           # 单一入口 → PASS/FAIL
├── simulate_log.py             # 极端压测数据生成
└── log_analyzer.py             # 极端压测分析器
```

### 19.2 5 项验证全部 PASS

```
[REAL_PIPELINE]         PASS
[USER_FOCUS]            PASS   env_leak=0, invalid_output=0
[PHASE_B_SPEECH_LOCK]   PASS   overlap=0, vlm_interrupted=0
[PHASE_A_PATH_INTEGRITY] PASS
[EXTREME_STRESS]        PASS   warning_drop=0, order=0
```

---

## 二十、Pi 上机准备（Stage 2 Preflight）

### 20.1 结构化日志系统

新增 `observe/trace_logger.py`（JSONL 追加），完整覆盖生命周期：

- SUBMIT（speech_arbitrator.submit() 唯一入口）
- DROP_CANDIDATE → VLM_SCORE_SELECT → SELECT → PLAY

同一 trace_id 全链路闭合：从任务提交 → 队列状态标记 → 评分选择 → 调度选中 → 实际播放。Pi 实机运行时 trace.jsonl 自动生成，无需额外配置。

### 20.2 Pi 兼容 Patch（4 处修改）

| 文件 | 修改 |
|------|------|
| `main.py` | `cv2.imshow`/`waitKey` 改为条件调用（`DISPLAY` 或 `os.name=="nt"` → GUI；否则 key=-1） |
| `main.py` | 新增 `select.stdin` 输入/退出：终端输入 `a`+Enter → 启动 ASR，`q`+Enter → 退出 |
| `main.py` | 摄像头自适应：`CAMERA_SRC` 环境变量支持 CSI GStreamer，失败自动 fallback |
| `core/config_loader.py` | Profile 自动检测：Linux → "pi"，PC → "pc" |

### 20.3 测试文档

3 份可执行操作文档及 1 份上机前检查清单：`Pi上机操作手册.txt`、`测试流程说明.txt`、`日志回收说明.txt`、`PI_PRECHECK.txt`。所有命令可直接复制执行，所有判断可观测。

---

## 二十一、当前系统能力总结

### 安全关键

- WARNING 零丢失（多队列 + 保留槽 + Aging Boost）
- USER_FOCUS 期间 VLM 强保障（保留槽绕过三队列竞争）
- speech_lock 防重叠 + VLM 原子播放

### 调度完备

- 三队列独立策略（WARNING 永不丢 / VLM FIFO丢旧 + 评分策略层 / ENV 满拒新）
- 6 层优先级链 + 加权轮询 + VLM Aging Boost 防饥饿
- force_play 强制执行路径 + USER_FOCUS 保留槽
- E0 DROP_CANDIDATE 纯观测标记（零行为变更）

### 工程能力

- 工业级配置系统（YAML + profile 切换 + deep merge）
- 统一验证框架（5 项自动化检测，一键 PASS/FAIL）
- 评估流水线（analysis/pipeline.py，模拟→评估→存档→对比）
- 结构化日志链路（JSONL trace，SELECt 精确时间戳）
- Pi 兼容（headless patch + stdin 退出 + 测试文档）

---

## 二十二、观测层与评估层（Phase E0+ Anti-Fabrication）

### 22.1 trace_logger 结构化日志

新增 `observe/trace_logger.py`，以 JSONL 格式追加写入：

- SUBMIT → DROP_CANDIDATE → VLM_SCORE_SELECT → SELECT → PLAY
- 同一 trace_id 全链路闭合

### 22.2 SUBMIT 单一来源统一

确认 `speech_arbitrator.submit()` 是所有任务的唯一汇入点。在此处添加 SUBMIT trace（含 id/source/priority/ts），删除模拟器中旧重复日志。每个任务仅产生一次 SUBMIT，消除双重记录。

### 22.3 评估流水线 `analysis/pipeline.py`

一键执行：simulate_log → evaluate → archive → compare：

```bash
python analysis/pipeline.py
```

输出 summary.txt、report.json、diff.txt，自动归档到 history/。

### 22.4 Evaluate V3 → V4 演进

- V2: 拆分为 wait_select / wait_play
- V3: 确认系统为 lazy scheduling（select ≈ play），核心指标改为 wait_queue
- V4: 完全移除 full_run.jsonl 依赖，仅从 trace.jsonl 读取。解析 SUBMIT 和 PLAY 事件构建任务生命周期，计算 wait_queue = play_ts - submit_ts。嵌入硬断言 `data_source == "trace_only"`

### 22.5 Anti-Fabrication 可审计验证体系

新增 `analysis/recompute_from_trace.py`：与 evaluate_scheduler.py 完全独立的验证脚本。从 trace.jsonl 重新计算所有指标，与 report.json 逐项对比（容差 < 0.01s）。输出 `consistency_verdict.txt`（PASS/FAIL）。

核心原则：trace.jsonl 是唯一真实数据源（Single Source of Truth），所有指标必须 100% 可从 trace 独立重建。

当前验证结果：SUBMIT 410 条（无重复），PLAY 47 条，max_wait_queue=12.0s，avg_wait_queue=3.5s。recompute vs report 误差=0，consistency_verdict=PASS。评估报告具备可审计性，任一持有 trace.jsonl 的第三方可独立复现所有结论。

### 22.6 观测审计

完成全链路可观测性审计（analysis/observability_report.txt）：

- 所有未播放 VLM 条目均有 DROP_CANDIDATE 标记 ≠ 无静默丢失
- FIFO eviction 无独立 trace 事件（待增强）
- SELECT 精确时间戳已嵌入 arbitrator.select_next 所有返回路径

---

## 二十三、项目结构重构

完成系统分层重构：

```
edge-visionQA/
├── core/              # 系统主流程
├── perception/        # 感知 + 调度
├── vlm/               # VLM 异步管理
├── asr/               # 语音输入
├── tts/               # 语音输出
├── expression/        # 文本表达
├── observe/           # 观测层（trace_logger）← 新增
├── analysis/          # 评估层（pipeline, evaluate, compare）
├── config/            # 配置文件
├── tests/             # 验证体系
├── docs/              # 文档
├── main.py            # 唯一入口
└── config.py          # 兼容层
```

---

## 二十四、Pi 运行时适配（Rounds 1-4）

为确保系统在树莓派 5 上稳定运行并产生可评估日志，分四轮完成 14 项修复：

### Round 1：启动防崩溃

修复 4 项致命启动问题：

| 修复 | 文件 | 问题 |
|------|------|------|
| API key 缺失不崩溃 | `config.py` | `raise ValueError` → `print("[WARN]")` |
| VLM 禁用时 NameError | `controller.py` | `image` 变量提前赋值 |
| 模型缺失不崩溃 | `main.py` | YOLO / Piper 加载加 try/except |
| 全局异常兜底 | `main.py` | 外层 try/except + traceback |

### Round 2：ASR 输入容错

修复 keyboard 权限问题，保证无 sudo 环境可输入：

| 修复 | 文件 |
|------|------|
| `import keyboard` 改为 `try/except Exception` + 运行时 `keyboard.is_pressed('enter')` 权限测试 | `asr_utils.py`、`asr_local_utils.py` |
| ASR 双引擎失败后 `input()` stdin fallback | `asrmanager.py` |

效果：Pi 上无 keyboard 权限时自动降级为 stdin 输入，不崩溃。

### Round 3：TTS 播放保活

修复播放静默失败和死循环：

| 修复 | 文件 |
|------|------|
| 新增 `play_audio_file()`：Windows → `start`，Linux → `aplay` | `tts_local_utils.py` |
| pygame 初始化 `except ImportError` → `except Exception`，`playsound` 导入脱离 except 分支 | `tts_utils.py` |
| 6 个播放路径全部 try/except + `play_audio_file` fallback | 双文件 |
| pygame busy-wait 加 10s 超时 + sleep(0.05) | 双文件 |

效果：任意环境（PC/Pi）均可发声，pygame 失败自动回退到系统播放器。

### Round 4：Pi 运行阻塞解除

修复 5 项 Pi 专有阻塞问题：

| 修复 | 文件 | 问题 |
|------|------|------|
| 摄像头自适应 | `main.py` | `CAMERA_SRC` env：CSI→GStreamer pipeline，USB→整数，失败→fallback |
| 丢帧不退出 | `main.py` | `cap.read()` 失败时 `break` → `continue` |
| Profile 自适应 | `config_loader.py` | `platform.system()=="Linux"` → 默认 `"pi"` |
| Headless 输入 | `main.py` | stdin `a`+Enter → `async_asr.start_listening()` |
| YOLO 参数 CONFIG 化 | `main.py` | `conf_threshold=0.5,imgsz=320` → `CONFIG.get("yolo.conf_threshold/imgsz")` |

### 当前状态

系统可在 Pi 5 上通过 `CAMERA_SRC=csi python main.py` 启动运行，支持：
- CSI / USB 摄像头自适应
- headless stdin 输入（SSH 终端 `a`+Enter 触发 ASR）
- TTS 多层 fallback（pygame → aplay）
- 自动 Pi profile 加载
- 完整 SUBMIT→SELECT→PLAY trace 生成

日志评估就绪：trace.jsonl 可独立交付，通过 `evaluate_scheduler.py` + `recompute_from_trace.py` 分析验证。

---

---

## 二十五、验证就绪审计 + Stage 1 调度验证 + 文档体系重构

### 25.1 验证前 Preflight Audit

对系统进行了"验证就绪性审计"，判断是否具备三阶段逐层验证条件。审计四个维度：

| 维度 | 结论 | 关键发现 |
|------|------|---------|
| Trace 完整性 | ✅ 通过 | SUBMIT/SELECT/PLAY 全链路覆盖，TraceLogger 不受 enable_logging 开关影响 |
| 输入可控性 | ✅ 通过 | simulate_log.py 提供虚拟时钟+精确任务注入 |
| 系统稳定性 | ⚠️ 有风险 | SpeechManager 消费线程和 VLMManager 调度线程缺少 try/except |
| 降级能力 | ✅ 通过 | 摄像头/麦克风/pygame/YOLO 四个 fallback 路径全部在位 |

审计结论：**READY WITH RISKS**。未发现阻断项，但两处 daemon 线程缺乏异常保护需修复。

### 25.2 调度闭环完整性修复

**问题**：`controller.py:_drain_arbitrator()` 中 `select_next()` 弹出的任务若被 `speech_lock` 阻断，`try_play()` 返回 False 后直接 `return`，不记录任何 trace 事件。该任务已被从队列弹出，却无 PLAY 或 PLAY_FAIL 记录，形成 silent drop。

**修复**：

```python
try:
    success = self.speech.try_play(item)
except Exception as e:
    success = False
    self.arbitrator.trace.log("PLAY_FAIL", id=..., reason=f"exception:{e}")

if not success:
    self.arbitrator.trace.log("PLAY_FAIL", id=..., reason="speech_busy")
    return
```

同时为两个缺少异常保护的 daemon 线程添加 try/except：
- `speech_manager.py:_run_consumer` — 消费线程静默死亡保护
- `vlm_manager.py:_scheduler_loop` — 调度线程静默死亡保护

**验收**：`simulate_log.py` + `run_validation.py` 5/5 PASS。

### 25.3 文档体系重构

将分散在 4 份文档中的重复"系统行为说明"抽取为独立引用，对齐三份 Stage 操作标准的模板结构：

| 文档 | 原状态 | 现状态 |
|------|--------|--------|
| 系统行为说明.txt | 不存在 | **新建**，公共参考 |
| Stage 1 操作标准（stdin 调度验证） | 草稿 | 对齐 + Pi headless 交互 + SCP 日志回收 |
| Stage 2 操作标准（视觉负载验证） | 不存在 | **新建**，同模板 |
| Stage 3 操作标准（实地语音测试） | 测试流程说明.txt | 同模板重构 |
| Pi上机操作手册.txt | 含重复系统行为段 | 精简引用 |
| PI_PRECHECK.txt | 含重复系统行为段 | 精简引用 |

清理了项目中残留的旧评测输出、`__pycache__`、`analysis/history/` 历史存档。更新 `.gitignore` 添加 `analysis/*.txt`、`analysis/report.json`、`MID_REPORT_DRAFT.txt`、`大创日志.md`，提交并推送。

### 25.4 Stage 1 自动化实验

创建 `scripts/run_stage1_direct.py`（直接驱动 SpeechArbitrator，不依赖 GUI/stdin/cv2.waitKey），以及 `scripts/run_stage1_multi.py`（多轮执行 + 自动一致性检查 + CSV 输出）。

**三轮实验结果（3 × 3 = 9 条 trace，全部精确相等）**：

| 负载 | 提交数 | PLAY | 播放率 | avg_wait |
|------|--------|------|--------|----------|
| low (5s) | 24 | 24 | 100.0% | 0.05s |
| mid (2s) | 60 | 1 | 1.7% | 0.05s |
| burst (10×3) | 30 | 3 | 10.0% | 0.11s |

### 25.5 关键机制发现：节流耦合

在分析 mid 负载 1.7% 播放率时发现一个重要的调度策略特性。

**机制**：`select_next()` 在加权轮询路径中调用 `_pop_vlm()`，该函数内部设置 `self.last_play_time = now`。随后立即进入 `_apply_throttle()` 检查 `now - last_play_time >= 1.5s`。由于两个操作的 `now` 相差微秒级，条件永远为 false。

**结论**：这是调度器更新与检查之间的耦合，不是性能瓶颈。系统策略为"低延迟优先"——高负载下丢弃任务而非排队，保证 max_wait 始终在 0.1s 级别。该行为在三轮重复实验中完全一致，确认是确定性策略结果。

### 25.6 当前状态

项目已从"开发测试阶段"进入"正式验证阶段"。Stage 1 基线已建立，Stage 2（视觉负载）和 Stage 3（语音交互）的验证规范已就位。累计文档 11 份，自动化脚本 2 个，评估管线完整。

---

## 二十六、Stage 2 Controller 验证 + Stage 3 全链路 + 三阶段统一结论

### 26.1 Stage 2：Controller 全链路与 USER_FOCUS 验证

创建 `scripts/run_stage2_controller.py`，通过 `Controller.handle_event()` 注入 navigation 和 user_input 事件，验证完整调度链中的 USER_FOCUS 机制。使用两套交替物体（前方行人 / 右侧汽车）触发场景变化，在 t=10s/40s/80s 注入用户提问。

实验结果：三轮完全一致。导航事件仅产生 1 次 SUBMIT 即被 `throttled_drop_env` 丢弃，user_input 全部 9 次成功播放（avg_wait≈0.005s）。结论：USER_FOCUS 机制在当前节流参数下未被独立验证——导航事件在到达 USER_FOCUS 判定路径前已被节流耦合杀死。节流耦合是全局性的，不仅影响 VLM（priority=2）也影响 ENV（priority=3）。

### 26.2 Stage 3：真实 main.py 全链路运行

在 PC 上运行 `main.py`，摄像头对准桌面场景（含书、水杯等物体），通过终端手动输入 4 种用户提问，持续 5 分钟，产出 179 条 trace 事件。

结果与传统合成实验形成鲜明对比：

| 来源 | SUBMIT | PLAY | 播放率 |
|------|--------|------|--------|
| decision（导航） | 20 | 12 | 60.0% |
| vlm（视觉问答） | 9 | 9 | 100.0% |
| user_direct | 42 | 15 | 35.7% |

avg_wait=0.1s, max_wait=0.8s, PLAY_FAIL=0, warnings_dropped=0。

与 Stage 1/2 的 0% 导航存活率形成极端对比：在真实帧循环中，drain 间隔被帧率+推理开销自然拉开，节流耦合不再"必杀"。此外，真实 YOLO 输出的连续扰动（每帧微幅波动）是合成实验两套静态 objects 离散切换无法复现的。

**核心发现**：导航事件生成 ≠ 按固定频率触发；导航事件生成 = f(视觉输入扰动)。调度器行为不仅取决于调度策略，还显著依赖于上游感知模块的输出特性。

### 26.3 Stage 3A+ 参数扫描与合成输入局限性

创建 `scripts/run_stage3a_plus.py`，通过控制主循环节奏（sleep注入）模拟 Pi 帧处理延迟，扫描 8 种 delay × interval 组合。结果：导航事件全部为 0%（单一 SUBMIT 被节流杀死），无任何区分度。

失败原因不在脚本设计，而在合成输入模型过于简化：两套静态 objects 的离散切换缺少真实 YOLO 的连续扰动分量——边界框微移、置信度波动、方向抖动——这些高频变化正是驱动 DecisionMaker 持续产生导航事件的物理来源。

### 26.4 三阶段统一结论

三阶段实验的共同结论被归纳为一份统一文档 `docs/三阶段实验统一结论与系统行为解释.txt`：

- 系统行为不是调度策略的单变量函数，而是**调度机制 × 输入事件分布**的联合结果
- 节流耦合在连续 drain 环境中（合成测试）杀死所有导航事件，在帧间隔 drain 环境中（真实运行）自然缓解
- 系统可用性由真实 YOLO 输出的连续扰动保证，这是一种"自然负载均衡"
- Pi 上的行为是非线性的：帧率降低可能改善（间隔增大）也可能恶化（队列堆积），需实机验证
- 当前不应修改调度参数——在获得 Pi 实机数据之前，参数优化缺乏决策依据

### 26.5 当前状态

三阶段验证全部在 PC 上闭环。产出 3 份实验报告 + 1 份统一结论文档 + 4 个自动化实验脚本。项目已从"开发阶段"进入"答辩准备阶段"。下一步唯一必要工作是 Stage 3B（Pi 实机验证），用于确认现有行为规律是否在边端条件下成立。

*日志更新日期：2026-05-06*

---

## 二十七、E1 工程收敛（观测/配置/线程/验证）

### 27.1 E1 执行概览

在完成三阶段验证后，系统进入 E1（Engineering Closure Phase 1）工程收敛阶段，目标是将从"行为正确但工程不可控"推进到"可观测、可控制、可验证"的状态。E1 分 7 步独立执行，每步一个 commit：

| Step | 改动 | 效果 |
|------|------|------|
| 1 | arbitrator 6 个 DROP/REQUEUE print → trace.log | 丢弃事件首次可统计 |
| 2 | VLMManager trace 注入 | queue_full 可追溯 |
| 3 | 4 参数接 CONFIG | decision_utils + output_policy 参数可控 |
| 4 | speech_lock 加锁 + 线程注释 | 竞态消除 |
| 5 | 延迟 base64 编码 + paplay fallback | Pi 性能改善 |
| 6 | 新验证器（DROP coverage + trace completeness） | 验证体系补强 |
| 7 | sys.path 删除（4 文件） | import 路径规范化 |

### 27.2 E1.5：SUPPRESS 事件 + 生产路径纯净化

审计发现 OutputPolicy 和 DecisionMaker 的 suppression 路径完全不可观测——任务被静默丢弃，trace 中不留任何痕迹。E1.5 引入 SUPPRESS 事件类型：

- OutputPolicy.allow() → 返回 (bool, reason) tuple
- DecisionMaker.get_decision() → 返回 dict 含 suppress_reason 字段
- Controller 统一写入 SUPPRESS(id, stage, reason)
- SIMULATE_PI 代码从 vlm_manager 和 speech_arbitrator 中删除
- VLM FIFO eviction 增加 trace.log("DROP", reason="vlm_fifo_eviction")

E1.5 后审计：可观测性、可解释性、生产路径纯净性三项从 FAIL 转为 PASS。

### 27.3 后 E1 清理（Pre-Deployment Closure）

进一步清理：

- 5 个遗漏的 trace.log() 缺口（VLM_DROP empty_answer / version_mismatch、USER_FOCUS_BLOCK、STARTUP_TRIGGER、USER_QUERY）
- 绝对路径从 anti_fabrication_check.py 移除
- 删除死调度函数 can_play_vlm()（0 调用）
- 删除死代码：ExpressionStrategy、is_scene_changed、get_dialog_history、update()（-76 行）
- 8 个 YAML 键接入 CONFIG（cold_start.duration、user_focus 超时/窗口、persistence_threshold、history_limit、min_interval、stable_count）

最终系统达到"可审计"标准：任何"没播放"的情况在 trace 中必有 SUPPRESS / DROP / PLAY_FAIL 之一。实验脚本（scripts/）从仓库跟踪中移除，仓库只保留核心系统代码。

### 27.4 当前状态

项目完成工程收敛。核心代码 60+ 文件，行数约 5000 行。三阶段验证 + 工程收敛全部在 PC 上闭环。等待 Pi 实机验证（Stage 3B）确认趋势即可完成全部设计验证。

*日志更新日期：2026-05-06*

---

## 二十八、Expression 层增强：导航导引 + 风险提示 + 场景记忆

### 28.1 导航语义升级

在 Expression 层新增 `expression/navigation_advisor.py`，实现基于物体方向的避让建议生成。将 ENVIRONMENT_DESC 和 APPROACHING 模板分为 DESCRIPTIVE（描述）/ GUIDED（导引）/ GUIDED_DANGER（危险导引）三层：

- 较远物体：使用 DESCRIPTIVE 模板（如"右侧有汽车"）
- 较近一般物体：使用 GUIDED 模板（如"前方有行人，建议稍向右侧移动"）
- 较近危险物体（汽车/卡车/公交/摩托车）：使用 GUIDED_DANGER 模板（如"前方较近处有汽车，请注意避让，向右侧移动"）
- EMERGENCY_WARNING 保持不变（安全优先）

改动局限在 `expression/` 目录，SUBMI%/SELECT 数量与 trace 结构完全不变。

### 28.2 场景上下文记忆

在 `Context.scene` 中新增 `recent_objects` 缓存（30 秒滑动窗口），记录最近出现过的物体（class_zh、direction、distance、时间戳）。去重取前 3 个，通过 `build_vlm_context` 注入 VLM 的 system prompt，为指代消解提供上下文支持。

### 28.3 安全性审查

确认 GitHub 仓库无 API 密钥泄露：`config.py` 仅通过 `os.environ.get` 读取环境变量，所有模型名和 URL 均为公开信息。

### 28.4 当前状态

三阶段验证 + E1 工程收敛 + 表达层增强完成。系统具备调度确定性、可观测性闭环、导航语义升级和场景记忆能力。下一步唯一必要工作为 Stage 3B 树莓派实机验证。

---

## 二十九、节流耦合修复 + 表达层导航升级 + 场景记忆

### 29.1 节流耦合根本性修复

Stage 1 发现的核心机制——`_pop_vlm`/`_pop_env`/`_pop_warning` 更新 `last_play_time` 后立即被 `_apply_throttle` 检查导致差值 ≈ 0 ——被彻底修复。修复方案：将 `last_play_time` 的写入从各个 `_pop_*` 函数移除，改为在 `select_next()` 的节流门控通过之后统一写入。

效果：
- 系统启动后第一条导航不再被吞（启动消息的 `last_play_time` 远在几秒前）
- `last_play_time` 现在反映真正播放的时刻，而非选取尝试的时刻
- VLM 保活路径的 `last_vlm_play_time` 保持不变
- 改动量：4 行删除 + 1 行新增，仅 `speech_arbitrator.py`

### 29.2 表达层导航导引升级

新增 `expression/navigation_advisor.py`，实现基于物体方向的避让建议生成。`ExpressionEngine` 根据物体距离和危险等级自动选择三层模板：

- DESCRIPTIVE（较远）："右侧有汽车"——纯描述，不导引
- GUIDED（较近+非危险）："前方有行人，建议稍向右侧移动"——加避让建议
- GUIDED_DANGER（较近+危险物）："前方较近处有汽车，请注意避让，向右侧移动"——强调安全

危险物判定复用 `spatial_utils.DANGER_CLASSES`（汽车/卡车/公交/摩托车），EMERGENCY_WARNING 不受影响。模板也修复了 `{avoid_dir}` 为空时导致残缺句的问题（用两套模板分支而非空字符串塞入同一模板），以及"可以向右侧稍微"缺动词的问题（→"可以稍向右侧移动"）。

### 29.3 场景上下文记忆

在 `Context.scene` 中新增 `recent_objects` 字段（30 秒滑动窗口），每次场景稳定时记录出现过的物体（class_zh/direction/distance/ts），去重取前 3 个。通过 `build_vlm_context` 注入 VLM 的 system prompt，为用户指代问题提供语境支持。

例如用户问"他还在吗"，VLM 的 system prompt 中带有"最近环境中出现过：前方的行人、右侧的汽车"，VLM 可以据此推断"他"指代"行人"。

### 29.4 当前状态

节流耦合修复解决了 Stage 1 发现的核心机制问题。表达层从纯描述升级为描述+导引+危险分级三层输出。场景记忆为 VLM 提供了对话连续性。代码改动全部局限在 `speech_arbitrator.py`（1行）、`expression/`（3文件）、`controller.py`/`vlm_manager.py`（2文件），未触及调度策略和 trace 结构。



## 三十、冷启动窗口修复 + 第一条导航吞没问题根治

### 30.1 问题发现

系统启动后第一条导航播报（"前方出现行人，可以稍向右侧移动"）始终被吞。终端可见 SUBMIT 记录但从未听到播报。现象在三次不同类型的修复后依然存在，最终确认根因为多层时间线叠加。

### 30.2 第一轮修复：throttle 耦合修复

`_pop_*()` 在选中任务时设置 `last_play_time = time.time()`，`_apply_throttle()` 立即用同一时钟比较，差值在微秒级永远 < 1.5s。修复：删除全部 `_pop_*` 中的 `last_play_time` 赋值，改为在 `select_next()` 节流门控通过后统一写入。

### 30.3 第二轮修复：冷启动起算点前移

`cold_start_start_time` 原在 `__init__` 设置，模型加载+启动播放耗时消耗了冷启动窗口。修复：移至 `_play_startup_message()` 末尾，从系统真正就绪开始计时。

### 30.4 第三轮修复：缩短启动消息 + 延长窗口

播报文本从 22 字缩短为 5 字（去掉"按A按Q"），播放时长从 ~2.5s 降至 ~0.5s，同时适配 Pi 无键盘场景。cold_start.duration 从 2.0s 延长至 5.0s，覆盖 YOLO 预热与安全余量。

### 30.5 VLM prompt 优化

将系统初始 prompt 替换为更口语化、更严格的格式约束："你是盲人视觉助手。直接以一句话口语回答用户问题，不加标题、序号、括号。禁止使用'图片''画面''图像''根据'等词汇。" 场景记忆注入改为"最近你在环境中看到过"。token 数从 42 降至 28。

### 30.6 TTS 临时文件路径优化

将 TTS 播报的临时 wav 文件从 `tempfile.gettempdir()`（Pi 上为 `/tmp`，写入 SD 卡）迁移至 `/dev/shm`（Linux RAM disk），Windows 保持原路径不变。每次播报约 200KB 的读写操作不再消耗 SD 卡寿命。改动仅 `tts_local_utils.py` 3 行。

### 30.7 VLM 模型升级 qwen3.6-flash

将 VLM 模型从 `qwen3.5-flash` 升级至 `qwen3.6-flash`（阿里云 DashScope），新一代模型空间位置和指代消解理解更强，更适合盲人导航场景。配置改动仅 `config.yaml` 1 行，同步更新 README 和技术手册中的模型标识。

### 30.8 VLM prompt 优化

替换系统初始 prompt 为更口语化、更严格的格式约束，增加"禁止使用'图片''画面''图像''根据'等词汇"规则，强制一句话口语回答。token 数从 42 降至 28。

### 30.9 Pi 部署优化（P0 级三处修复）

针对树莓派 5 部署场景进行三项不影响调度行为的纯工程优化：

- **TraceLogger 内存 buffer**：将逐条 `open→write→close` 改为 20 条或 2 秒间隔的 buffer flush，消除 SD 卡高频随机写入。SD 卡写入量从每秒数十次降至每 2 秒一次。
- **PipeWire 音频 fallback**：`tts_local_utils.py` 播放链加入 `pw-play`，覆盖 Pi OS Bookworm+ 默认的 PipeWire 音频栈。fallback 顺序：`aplay → paplay → pw-play`。
- **OpenAI client 复用**：`vlm/providers/vlm_utils.py` 的 OpenAI client 改为模块级单例，复用 TLS session，每次 VLM 调用节省 100-300ms。
- **网络探测端点修正**：`runtime_manager.py` 的网络探针从 `8.8.8.8:53`（DNS 端口）改为 `dashscope.aliyuncs.com:443`（业务端点），避免校园网/企业网封锁 DNS 53 导致系统误判为离线。

*日志更新日期：2026-05-07*

---

## 三十一、Phase 2-3：Camera Runtime 解耦 + Pi5 Runtime Baseline

### 31.1 Pi5/Trixie Camera 兼容性问题诊断

前期在 Pi5/Trixie 上部署时发现系统无法正常使用摄像头。经排查，根因并非此前猜疑的 arbitrator、trace、scheduler 或 controller 模块，而是：**OpenCV VideoCapture backend 与 libcamera runtime 在 Pi5/Trixie 环境下存在兼容性问题**。同环境下的 Picamera2(Python) 最小 runtime 已独立验证通过，确认了换用 Picamera2 作为正式 Camera backend 的可行性。

### 31.2 CameraProvider 架构抽象（Phase 2）

新增 `camera/` 目录，实现 Camera Runtime 与业务层的彻底解耦：

```
camera/
├── base_provider.py   # 抽象接口: start()/read()/stop()/is_opened()
├── pc_provider.py     # 封装 cv2.VideoCapture（含 CSI/GStreamer/V4L2 fallback 链）
├── pi_provider.py     # 封装 Picamera2（懒加载 import，PC 环境不崩溃）
└── factory.py         # 复用 CONFIG profile 判断，自动返回 PCProvider/PiProvider
```

**接口契约**：`read()` 返回 `(success: bool, frame: numpy.ndarray)`，frame 始终 BGR 格式——YOLO、cv2.imencode、HUD overlay、OpenCV GUI 全部依赖此契约。

**关键设计决策**：
- PiProvider 的 `from picamera2 import Picamera2` 放在 `start()` 方法内部（懒加载），PC 环境 import 链路不触发 → 避免整个项目因缺少 picamera2 依赖而崩溃
- PCProvider 将 `main.py` 中原有的 4 级 fallback 链（CSI→GStreamer pipeline→V4L2→默认设备0）全部内聚到 provider 内部，调用方零感知
- PiProvider 显式配置 `main={"format": "BGR888"}` 确保 frame 兼容下游；`capture_array()` 直接返回 BGR numpy array
- Factory 复用现有 `CONFIG.get("profile")` 判断逻辑，不引入新的平台检测体系

### 31.3 main.py 最小改动

仅替换 3 处——Camera 初始化、`cap.read()`、`cap.release()`。`cv2.imencode`、`cv2.imshow`、`cv2.waitKey`、`cv2.destroyAllWindows` 完全不变。YOLO、VLM、ASR、arbitrator、trace、scheduler 模块零修改。

新增依赖：`picamera2`（Pi 端，PC 环境 pip install 会失败，属正常行为）。

### 31.4 Runtime Feature Flag 体系（Phase 3 基础设施）

为支持逐层恢复验证（Phase 3-8），建立 5 个独立 feature flag，全部默认 `"1"`（不加环境变量时行为与改前完全一致）：

| Flag | 控制范围 |
|------|---------|
| `ENABLE_YOLO` | YOLO 模型加载、推理、`draw_boxes` overlay |
| `ENABLE_CONTROLLER` | Controller 构造/销毁、nav_event 分发、arbitrator/VLM drain、启动播报、日志保存 |
| `ENABLE_ASR` | ASRManager 构造/销毁、语音采集、`a` 键触发（依赖 Controller 分发事件） |
| `ENABLE_VLM` | VLM 功能入口（当前已定义，Phase 8 正式启用） |
| `ENABLE_TTS` | TTS 模型加载（播放链路在 SpeechManager 内部，Phase 6 需额外 mock 改造） |

gating 覆盖范围：模型加载块、Controller/ASR 构造块、主循环 YOLO 检测块+nav_event 块+ASR poll 块+VLM drain 块+key 处理块、finally 清理块。全部用 `if ENABLE_*:` 守卫，Controller/ASR 未构造时对应变量为 `None`，finally 块独立判空避免 `AttributeError`。

### 31.5 Phase 3：Pi5 Runtime Baseline 验证

**目标**：验证 PiProvider + main loop + GUI 能否长期稳定运行。不测试任何 AI 功能。

启动方式（Pi 终端 bash，非 PowerShell）：
```
ENABLE_YOLO=0 ENABLE_CONTROLLER=0 ENABLE_ASR=0 ENABLE_VLM=0 ENABLE_TTS=0 python main.py
```

仅启动：CameraProvider(PiProvider/Picamera2) + main loop + GUI。不启动：YOLO、Controller、Arbitrator、ASR、VLM、TTS。

**验证清单**：
1. 确认 PiProvider 激活 → 终端输出 `[CAMERA] PiProvider active`
2. GUI 持续刷新 → 无黑屏/停帧
3. 10-30 分钟长时间稳定性 → 无 freeze/GUI stall/latency drift
4. `q` 键正常退出 → 无 camera busy/unavailable 错误
5. CPU/内存观察（`htop`）→ 无异常增长

**产出**：`Pi5_Phase3_Handbook.txt` 完整操作手册（含依赖安装、启动命令、验证清单、FAQ、Phase 4 预告）。同时为 `pi_provider.py` 和 `pc_provider.py` 添加 `[CAMERA] *Provider active` 识别日志。

### 31.6 后续路径（Phase 4-8 渐进恢复计划）

Phase 4 — YOLO Runtime Recovery：恢复 YOLO 检测 + overlay + GUI，验证 Pi5 实时视觉负载（FPS、latency drift、inference block）。不恢复 Controller/Speech/ASR/VLM。

Phase 5 — Controller Decision Recovery：恢复 scene update/decision logic/context。不恢复语音。

Phase 6 — SpeechArbitrator Recovery：恢复事件调度/优先级/抢占。mock TTS（`print("[MOCK TTS]", text)`）。

Phase 7 — TTS Runtime Recovery：恢复真正音频，验证 ALSA/PulseAudio/PipeWire/queue/interruption。

Phase 8 — VLM/ASR Recovery：恢复网络/API/timeout/多线程/阻塞——最不稳定层最后接入。

*日志更新日期：2026-05-11*